# Enable a provider by setting its credential. Unset providers stay off.
STRIPE_API_KEY=
HUBSPOT_ACCESS_TOKEN=
# Inline JSON or a file path
GOOGLE_SERVICE_ACCOUNT_JSON=
# Write tools are OFF by default. Set to true to register them.
CONNECTOR_ALLOW_WRITES=false
